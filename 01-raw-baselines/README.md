# Raw Baselines

This layer records factual baselines and their provenance.

Rules:

- Treat this layer as read-only by default.
- Do not publish raw leaked source contents here.
- Record duplicate baselines explicitly instead of pretending they are independent evidence.
