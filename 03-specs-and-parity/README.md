# Specs And Parity

This layer stores the unified runtime concepts, schema, parity reports, and comparison matrices derived from the recorded sources.

Rules:

- Reference evidence instead of embedding copied source.
- Keep conflicts visible instead of silently flattening them.
